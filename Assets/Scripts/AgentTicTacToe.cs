using System.Collections.Generic;
using UnityEngine;

[System.Serializable]
public class AgentTicTacToe
{
    // Policy ? Ou le jeu donne la policy ?
    
    // Faut un tableau d'etat et leur action possible ?
    
    public float FirstVisitMCPrediction(object policy, int episodeCount = 10)
    {
        float V = 0.0f;
        
        return V;
    }
    
    public float EveryVisitMCPrediction(object policy, int episodeCount = 10)
    {
        float V = 0.0f;
        
        return V;
    }
}